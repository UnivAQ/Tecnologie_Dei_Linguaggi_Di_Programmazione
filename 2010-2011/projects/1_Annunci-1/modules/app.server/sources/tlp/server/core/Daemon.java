package tlp.server.core;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

@Service
public class Daemon
{
        private final Logger _logger = LoggerFactory.getLogger(Daemon.class);

        public Daemon()
        {
        }
}
